image: node:14.0.0

before_script:
  - yarn install

test:
  stage: test
  cache:
    paths:
    - node_modules/
  script:
    - yarn test

deploy:
  stage: deploy
  cache:
    paths:
    - node_modules/

  script:
  - yarn build

